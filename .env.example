# The private hosted site uses platform-provided identity and DB/BUCKET bindings.
# Only the local Wrangler preview may use this development identity.
LOCAL_DEVELOPMENT=false
# Comma-separated authorized officer-enrollment emails; configure as a hosted secret.
OFFICER_EMAIL_ALLOWLIST=
